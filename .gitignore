.venv/
staticfiles/
.cache/
__pycache__/
*.pyc
db.sqlite3
db.sqlite3-journal
db.sqlite3-wal
db.sqlite3-shm
media/
.env
.env.secrets
secrets/
.biota_users.json
.biota_notification_settings.json
# Локальное демо графика (manage.py create_graph_demo_local)
local/
FETCH_HEAD

# Claude Code / локальные настройки IDE
.claude/

# Графики смен — хранятся только на сервере, не трекаются гитом
schedules/
# Резервные копии склада — только на сервере
inventory_backups/
# Резервные копии регламентов — только на сервере
regulations_backups/
